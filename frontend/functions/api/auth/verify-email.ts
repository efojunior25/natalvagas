import { hashOpaqueToken, json } from "./_utils";
interface D1Database { prepare: (query: string) => { bind: (...args: any[]) => { first: <T = any>() => Promise<T | null>; run: () => Promise<any> } } }
interface Env { DB?: D1Database }
export const onRequestPost = async ({ request, env }: { request: Request; env?: Env }) => { try { if (!env?.DB) return json({ success: false }, 503); const body: any = await request.json().catch(() => ({})); const hash = await hashOpaqueToken(String(body.token || "")); const record: any = await env.DB.prepare("SELECT id, user_id, expires_at FROM account_tokens WHERE token_hash = ? AND purpose = 'VERIFY_EMAIL' AND used_at IS NULL").bind(hash).first(); if (!record || Date.parse(record.expires_at) <= Date.now()) return json({ success: false, message: "Link inválido ou expirado." }, 400); const now = new Date().toISOString(); const used: any = await env.DB.prepare("UPDATE account_tokens SET used_at = ? WHERE id = ? AND used_at IS NULL").bind(now, record.id).run(); if (!used?.meta?.changes) return json({ success: false, message: "Link já utilizado." }, 409); await env.DB.prepare("UPDATE users SET email_verified = 1, updated_at = ? WHERE id = ?").bind(now, record.user_id).run(); return json({ success: true, message: "E-mail verificado." }); } catch { return json({ success: false }, 500); } };
