import React, { useEffect, useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
export const VerifyEmail: React.FC = () => { const [params] = useSearchParams(); const [message, setMessage] = useState('Verificando e-mail...'); useEffect(() => { const token = params.get('token'); if (!token) { setMessage('Link inválido.'); return; } fetch('/api/auth/verify-email', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token }) }).then(async r => ({ ok: r.ok, data: await r.json().catch(() => ({})) })).then(({ ok, data }) => setMessage(ok ? 'E-mail verificado com sucesso.' : (data.message || 'Não foi possível verificar o e-mail.'))).catch(() => setMessage('Serviço indisponível.')); }, [params]); return <main className="min-h-screen flex items-center justify-center bg-slate-50 p-4"><section className="max-w-md w-full bg-white border border-slate-200 rounded-3xl p-8 text-center shadow-sm"><h1 className="text-xl font-black mb-3">Verificação de e-mail</h1><p className="text-sm text-slate-600 mb-5">{message}</p><Link to="/" className="text-brand-600 font-bold text-sm">Voltar ao Natal Vagas</Link></section></main>; };
