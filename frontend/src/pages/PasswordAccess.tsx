import React, { useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
export const PasswordAccess: React.FC = () => { const [params] = useSearchParams(); const token = params.get('token'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); const submit = async (event: React.FormEvent) => { event.preventDefault(); const endpoint = token ? '/api/auth/reset-password' : '/api/auth/request-password-reset'; const body = token ? { token, password } : { email }; const response = await fetch(endpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json().catch(() => ({})); setMessage(data.message || (response.ok ? 'Solicitação concluída.' : 'Não foi possível concluir.')); }; return <main className="min-h-screen flex items-center justify-center bg-slate-50 p-4"><form onSubmit={submit} className="max-w-md w-full bg-white border border-slate-200 rounded-3xl p-8 shadow-sm space-y-4"><h1 className="text-xl font-black">{token ? 'Redefinir senha' : 'Recuperar acesso'}</h1>{token ? <input type="password" required minLength={12} value={password} onChange={e => setPassword(e.target.value)} placeholder="Nova senha forte" className="w-full p-3 rounded-xl border border-slate-200" /> : <input type="email" required value={email} onChange={e => setEmail(e.target.value)} placeholder="seuemail@exemplo.com" className="w-full p-3 rounded-xl border border-slate-200" />}<button className="w-full py-3 rounded-xl bg-brand-600 text-white font-bold">{token ? 'Salvar nova senha' : 'Enviar instruções'}</button>{message && <p className="text-sm text-slate-600">{message}</p>}<Link to="/" className="block text-center text-brand-600 font-bold text-sm">Voltar</Link></form></main>; };
